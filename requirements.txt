torch>=2.1.0
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
mpmath>=1.3
# optional — multi-modal organism (install as needed)
# pypdf>=4.0          # PDF page text extraction
# av>=10.0            # video/audio demux (PyAV)
# soundfile>=0.12     # audio windows
# faster-whisper>=1.0 # local speech→text (subtitle fallback)
# psutil>=5.9         # richer host interoception
# allensdk>=2.16.0    # richer Allen SDK (offline CSV works without it)
